#include<bits/stdc++.h>
using namespace std;

int main(){
    int n; cin >> n;
    int sz = 0;
    while(n--){
        string s; cin >> s;
        if(s.size() > sz) sz = s.size();
    }

    while(n--){
        
    }

    return 0;
}